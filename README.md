# Streaming Login UI Study

An educational static HTML/CSS recreation of a streaming-service login layout. It is intentionally a visual study only: it does not collect credentials, authenticate users, or claim affiliation with Netflix.

## Run locally

Open `index.html` in a browser or serve this directory with any static server.
